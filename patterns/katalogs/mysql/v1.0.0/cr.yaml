apiVersion: mysql.orkestra.io/v1
kind: MySQL
metadata:
  name: my-mysql
  namespace: default
spec:
  version: "8"
  database: myapp
  username: myapp_user
  storageSize: 10Gi
  storageClass: standard
  resourceProfile: standard
  replicas: 1
