apiVersion: mongodb.orkestra.io/v1
kind: MongoDB
metadata:
  name: my-mongodb
  namespace: default
spec:
  version: "7"
  database: myapp
  username: myapp_user
  storageSize: 10Gi
  storageClass: standard
  resourceProfile: standard
  replicas: 1
